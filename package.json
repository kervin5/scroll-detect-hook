{
  "name": "scroll-detect-hook",
  "version": "1.0.2",
  "description": "This is a hook for React that allows to detect when the window is scrolling and provides you with the direction and position",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react",
    "hook",
    "scroll",
    "window",
    "detect"
  ],
  "author": "kervin5",
  "license": "ISC",
  "dependencies": {
    "react": "^16.13.1"
  }
}
